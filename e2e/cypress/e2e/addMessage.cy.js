//TODO: COMMENTED OUT BECAUSE IT BREAKS THE PIPELINE, NEEDS TO BE IMPLEMENTED AFTER BUT CURRENTLY WRONG TEST FOR CURRENT PROJECT, PROBABLY NOT BEING USED IN FUTURE

/*context('add message', () => {
    let msgText = 'msg' + new Date().getTime();

    it('create message', () => {
        cy.loginAdmin();
        cy.createMessage(msgText);
    })

});*/
