logging:
  file.name: ./log/backend-test.log

spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    h2:
      console:
        enabled: true
  jpa:
    hibernate:
      ddl-auto: create
